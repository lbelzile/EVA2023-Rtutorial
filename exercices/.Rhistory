system2('updmap-sys')
tinytex::tlmgr_install("fourier")
tinytex::tlmgr_install("inputenc")
remotes::install_github("lbelzile/mev")
which <- "scale"
isTRUE(all(which %in% 1:2))
which <- 1
which <- c("scale","shape")[1:2]
which <- "scale"
which <- c("scale","shape")[which]
which <- "scale"
if(isTRUE(all(which %in% 1:2))){
which <- c("scale","shape")[which]
}
which <- match.arg(which, choices = c("scale", "shape"), several.ok = TRUE)
which
